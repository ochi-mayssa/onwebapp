[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
  "python3 -m pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "gunicorn websity_project.wsgi:application --bind 0.0.0.0:8000"
